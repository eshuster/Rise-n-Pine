$(document).ready(function(){
  $('.your-class').slick({
    slidesToShow: 1,
    slidesToScroll: 1,
    autoplay: true,
    autoplaySpeed: 5000,
    infinite: true,
    dots: true,
    // $slick-arrow-color: black,
    // centerPadding: '60px'
    // centerMode: true

  });

  $('.single-item').slick();

});

